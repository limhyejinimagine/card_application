
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/card_db_test?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: card_user
    password: card_pass
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        show_sql: true
  flyway:
    enabled: false

